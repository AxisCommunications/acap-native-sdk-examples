// Ignore directories which start with "build", but allow files.
// N.b. the dot '.' in the regexp is sensitive to being last in []
**/build/
**/build[a-z0-9._-]*/

// Ignore linter log files
super-linter.log*

// Ignore general files
.DS_Store
.bash*
package.conf*
param.conf
**/*_LICENSE.txt

// Ignore files from eap-install.sh
**/*var_log_messages.*
**/.eap-install.cfg
